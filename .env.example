DATABASE_URL="sqlite:///./originflow.db"
# Use the default SQLite database for local development. Comment this out and
# provide a PostgreSQL URL to switch to a different database engine.
TEMPORAL_HOST="localhost:7233"
API_KEY="your_octopart_api_key_here"
EMERGENCY_TOKEN="your_emergency_shutdown_bearer_token"
LOG_LEVEL="DEBUG"

import React from 'react';

const StatusBar = () => (
  <footer
    role="status"
    aria-live="polite"
    className="fixed bottom-0 left-0 w-full h-12 flex items-center px-6 bg-white border-t shadow text-sm z-50"
  >
    Status: Link Created
  </footer>
);

export default StatusBar;

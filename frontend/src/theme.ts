/**
 * Defines layout dimensions used throughout the application.
 * Centralized here to avoid hard-coded magic numbers in components.
 */
export const theme = {
  layout: {
    drawerWidth: 240,
    appBarHeight: 60,
    actionBarHeight: 48,
    footerHeight: 40,
  },
};
